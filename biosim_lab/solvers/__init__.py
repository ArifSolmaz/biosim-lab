"""Optional heavy numerical back-ends.

Nothing here is imported by the core. Every solver in this package is
discovered through the ``biosim_lab.solvers`` entry-point group and reports its
own availability, so Stages 1-3 run to completion with none of them installed.
"""
